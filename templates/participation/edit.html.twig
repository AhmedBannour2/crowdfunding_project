{% extends 'base.html.twig' %}

{% block title %}Modifier une Participation{% endblock %}

{% block body %}
    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow-lg p-4">
                    <h1 class="text-center mb-4">Modifier une Participation</h1>

                    {{ include('participation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                    <div class="d-flex justify-content-between mt-3">
                        <a href="{{ path('app_participation_index') }}" class="btn btn-secondary">Retour à la liste</a>
                        {{ include('participation/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
